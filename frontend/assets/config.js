/**
 * Site configuration — change support email and origin here.
 * Visible email links are synced by site.js via [data-support-email].
 */
window.DS_CONFIG = Object.freeze({
  siteName: 'Digital Streamers',
  siteOrigin: 'https://www.digitalstreamers.xyz',
  supportEmail: 'support@digitalstreamers.xyz'
});
